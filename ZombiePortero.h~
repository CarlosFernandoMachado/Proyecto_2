#pragma once
#include "Zombie.h"
#include <string>

using std::string;

class ZombiePortero{
	double puerta;
	
public:
	ZombiePortero(double=300, double=50, int=200, int=1, double=100);
	int getCosto();
	int getVelocidad_Avance();
	virtual double ataque();
	virtual double defensa(double);
	virtual string Descripcion();
};
